connection:
  host: localhost
  port: 3306
  name: test
  user: user
  database: test
  password: password
  driver: mysql
  dialect: mysql

schema:
  dir: database/schema
  directory: database/schema

backup:
  directory: database/backup

seed:
  dir: database/seed